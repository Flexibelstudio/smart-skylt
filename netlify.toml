[build]
  command = "npm install && npx vite build && if [ \"$BRANCH\" = \"staging\" ]; then printf '/*\\n  X-Robots-Tag: noindex\\n' > dist/_headers; fi"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200