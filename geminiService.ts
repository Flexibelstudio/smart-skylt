// This file is obsolete. All AI service calls are now routed through
// the secure proxy in /services/geminiService.ts, which calls the 
// 'gemini' Firebase Cloud Function. The exception is the HelpBot's
// streaming chat, which is also initialized via /services/geminiService.ts.
